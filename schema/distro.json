{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "WSL Kernel Distro Manifest Schema",
    "allOf": [
        {
            "$ref": "../../gaia/schema/distro.json"
        }
    ],
    "properties": {
        "machine": {
            "description": "The machine type",
            "type": "string",
            "enum": [
                "wsl-amd64",
                "wsl-arm64"
            ]
        }
    }
}
